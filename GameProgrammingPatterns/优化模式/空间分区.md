- 根据对象实际的**空间位置**在空间数据结构中进行存储，有效查询某处附近的对象。当对象位置改变时，更新它空间数据结构中的位置。
- 空间分区使用额外的内存记录划分的数据结构，**空间换时间**。
- **固定网格**：将单位按照固定的网格大小进行划分，同一个网格的单位都储存在同一个数据结构（集合、链表）中。
- **平面划分**更简单，内存使用量确定，对象改变位置时更新更快。
- **层次划分**更有效率，将空区域划分在一起，对象密集的区域划分更多。
- **四叉树**：总是将区域分成均等的四块，当一块区域中的对象数目超过了临界值，就将这块区域分成四块。
