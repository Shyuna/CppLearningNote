- 使用**双缓冲模式**的情况：需要维护一些被增量修改的状态。修改到一半时，状态可能会被外部请求。防止请求状态的外部代码知道内部的工作方式。读取状态时不想等着修改完成。
- 使用**缓冲类**封装缓冲，保存两个缓冲的实例：**当前缓冲**和**下一缓冲**。在当前缓冲执行读取操作，在下一缓冲执行修改操作。修改完成后，交换这两个缓冲。
- **交换操作**可以直接交换缓冲区的指针或将下一缓冲的数据拷贝到当前缓冲。拷贝操作需要耗费更多时间，但外部代码可以存储对缓存的永久指针。
